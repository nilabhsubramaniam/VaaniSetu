"""VaaniSetu's Python AI service package.

Phase 2, Milestone 2b adds exactly one capability module: `app.engines`
for the `llm` capability, behind the contract in
`proto/llm.openapi.yaml`. Later phases (3: asr, 4: tts, 7: rag) add
sibling capability modules here, per docs/DEVELOPMENT.md §5 — one module
per capability, never one growing into the others.
"""
